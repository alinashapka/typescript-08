interface FormValues {
        name: string;
        number: string;
};
    
   interface LoginForm {
        email: string;
        password: string;
};

interface RegistrationForm {
        name: string;
        email: string;
        password: string;
};